/*
    this script is just build for hzy.pw
    you can delete it safely
*/


! function(a) {
    function n() {
        var n = a("canvas.tagcloud-canvas");
        n.tagcanvas && n.tagcanvas("setspeed", [.2, -.1]);
    }
    setTimeout(n, 100);
}(jQuery);
